$('#avatarModal').modal('hide');

document.getElementById('imgAvatarLg').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('imgAvatarSm').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('modalImg').src = "<%= url_for(@user.user_profile.avatar) %>"

$.bootstrapGrowl('Imagem atualizada com sucesso!', {
  type: 'success',
  align: 'right',
  allow_dismiss: true,
  stackup_spacing: 10
});